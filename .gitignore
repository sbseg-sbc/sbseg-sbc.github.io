# Arquivos .txt permanecem apenas localmente (ex.: contatos.txt, dados pessoais)
*.txt

# Exceções: .txt que fazem parte dos mirrors históricos do arquivo SBSeg
!sbseg2012/**/*.txt

# Acervos pessoais com dados brutos de edições: permanecem apenas localmente
# (fontes das páginas reconstruídas, não devem ser publicados no GitHub)
dados_sbseg_2008/
dados_sbseg_2013/

# Cruft do sistema operacional
.DS_Store
Thumbs.db
